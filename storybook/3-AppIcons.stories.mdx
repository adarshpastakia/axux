import { Meta, Title } from "@storybook/addon-docs";
import { AxCopy, AxIcon } from "@axux/core";
import { AppIcons } from "@axux/core/src/types/appIcons";

<Meta title="AxUx/Icons" />

<Title>
  <section className="ax-font--serif ax-font--inherit ax-icon ax-bg--invert ax-color--invert ax-margin--e">
    <span>I</span>
  </section>
  Icons
</Title>

<div className="ax-flex">
  {Object.entries(AppIcons)
    .sort((a, b) => a[0].localeCompare(b[0]))
    .map(([key, icon]) => (
      <AxCopy text={key} key={key}>
        <section className="ax-align--center ax-paper ax-padding">
          <AxIcon icon={icon} size="lg" />
        </section>
      </AxCopy>
    ))}
</div>
