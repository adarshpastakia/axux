import { Meta, Title } from "@storybook/addon-docs";
import { AxCopy, AxIcon } from "@axux/core";
import { AppIcons } from "@axux/core/src/types/appIcons";

<Meta title="AxUx/Icons" />

<Title>
  <AxIcon className="ax-font--serif ax-margin--e ax-bg--invert" color="invert" icon="I" />
  Icons
</Title>

<div className="ax-flex">
  {Object.entries(AppIcons)
    .sort((a, b) => a[0].localeCompare(b[0]))
    .map(([key, icon]) => (
      <section key={key} className="ax-align--center ax-paper ax-padding">
        <AxIcon icon={icon} size="lg" />
      </section>
    ))}
</div>
