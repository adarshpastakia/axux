import { Title, Canvas, Story } from "@storybook/addon-docs";
import {
  AxDatePanel,
  AxRangePanel,
  AxSuperDate,
  AxDateDisplay,
  DateUtil,
} from "../src";
import { action } from "@storybook/addon-actions";
import { AxApplicationProvider } from "@axux/core";

export const presets = {
  "This Week": [
    DateUtil.startOfWeek(new Date()),
    DateUtil.endOfWeek(new Date()),
  ],
  "This Month": [
    DateUtil.startOfMonth(new Date()),
    DateUtil.endOfMonth(new Date()),
  ],
};

export const events = {
  2022: [
    {
      icon: "mdi mdi-calendar",
      label: "Event label",
      start: "2022-03-01",
      end: "2022-03-04",
    },
    {
      icon: "mdi mdi-bell",
      label: "Event label",
      start: "2022-08-12",
      end: "2022-08-15",
    },
    {
      icon: "mdi mdi-bell",
      label: "Event label",
      start: "2022-08-12",
      end: "2022-08-15",
    },
  ],
};

<Meta title="@date/Kitchensink" />

<Title>Date Kitchensink</Title>

<Canvas>
  <Story name="Kitchensink">
    <AxApplicationProvider>
      <div style={{ width: 800 }}>
        <AxSuperDate
          type="tag"
          showHijriToggle
          events={events}
          onChange={action("AxSuperDate:onChange")}
        />
        <div className="py-2" />
        <AxDateDisplay date={new Date()} />
        <div className="py-2" />
        <AxDatePanel
          showHijriToggle
          onChange={action("AxDatePanel:onChange")}
        />
        <div className="py-2" />
        <AxRangePanel
          showHijriToggle
          presets={presets}
          onChange={action("AxRangePanel:onChange")}
        />
      </div>
    </AxApplicationProvider>
  </Story>
</Canvas>
