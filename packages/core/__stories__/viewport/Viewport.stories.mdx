import {
  Anchor,
  ArgsTable,
  Canvas,
  Description,
  Heading,
  Meta,
  Story,
  Title
} from "@storybook/addon-docs";
import { ViewportStory } from "./ViewportStory";
import { AxViewport } from "../../src";

<Meta title="@core/Viewport" component={AxViewport} parameters={{ jest: ["Viewport.test.tsx"] }} />

<Anchor storyId="core-viewport--viewport">
  <Title>Viewport</Title>
</Anchor>

<Description of={AxViewport} />

<Canvas>
  <Story name="Viewport" height="30rem" parameters={{ layout: "none" }}>
    {ViewportStory}
  </Story>
</Canvas>

<Heading>Props</Heading>

<ArgsTable
  sort="requiredFirst"
  components={{
    AxViewport,
    "AxViewport.Header": AxViewport.Header,
    "AxViewport.Footer": AxViewport.Footer,
    "AxViewport.Banner": AxViewport.Banner,
    "AxViewport.Menu": AxViewport.Menu
  }}
/>
