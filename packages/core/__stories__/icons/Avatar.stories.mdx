import {
  Anchor,
  ArgsTable,
  Canvas,
  Description,
  Heading,
  Meta,
  Story,
  Title
} from "@storybook/addon-docs";
import { AxAvatar, AxFlexBox, AxIcon } from "../../src";
import { BasicStory, IconStory, ImageStory, SizeStory } from "./AvatarStory";
import {
  mdiAccount,
  mdiAccountVoice,
  mdiAlert,
  mdiBell,
  mdiBullseye,
  mdiCardAccountPhone,
  mdiFaceRecognition,
  mdiFlask,
  mdiNumeric1Circle
} from "@mdi/js";
import { PALETTE, THEME } from "../../../../storybook/stylings/theming-items";

<Meta title="@core/Icons/Avatar" component={AxAvatar} parameters={{ jest: ["Avatar.test.tsx"] }} />

<Anchor storyId="core-icons-avatar--avatar">
  <Title>Avatar</Title>
</Anchor>

<Description of={AxAvatar} />

<Canvas>
  <Story name="Avatar">{BasicStory}</Story>
  <Story name="Icon">{IconStory}</Story>
  <Story name="Image">{ImageStory}</Story>
</Canvas>

<Anchor storyId="core-icons-avatar--sizes">
  <Heading>Sizes</Heading>
</Anchor>

<Canvas>
  <Story name="Sizes">{SizeStory}</Story>
</Canvas>

<Anchor storyId="core-icons-avatar--infographs">
  <Heading>Infographs</Heading>
</Anchor>

<Canvas>
  <Story name="Infographs">
    <AxAvatar
      title="Username"
      icon={mdiAccount}
      size="5rem"
      className="ax-margin"
      infograph={{
        top: mdiBell,
        topEnd: <AxIcon bg="danger" color="white" icon={mdiBullseye} />,
        bottomStart: mdiAccountVoice,
        start: mdiCardAccountPhone,
        end: mdiFlask,
        bottom: <AxIcon bg="warning" color="white" icon={mdiAlert} />,
        topStart: <AxIcon bg="info" color="white" icon={mdiNumeric1Circle} />,
        bottomEnd: mdiFaceRecognition
      }}
    />
  </Story>
</Canvas>

<Anchor storyId="core-icons-avatar--themes">
  <Heading>Themes</Heading>
</Anchor>

<Canvas>
  <Story name="Themes">
    <AxFlexBox>
      <AxFlexBox.Row>
        {Object.values(THEME).map((color) => (
          <AxFlexBox.Col key={color} flex="auto">
            <AxAvatar title="Username" icon={mdiAccount} color={color} />
          </AxFlexBox.Col>
        ))}
      </AxFlexBox.Row>
      <AxFlexBox.Row>
        {Object.values(THEME).map((color) => (
          <AxFlexBox.Col key={color} flex="auto">
            <AxAvatar title="Username" icon={mdiAccount} bg={color} />
          </AxFlexBox.Col>
        ))}
      </AxFlexBox.Row>
      <AxFlexBox.Row>
        {Object.values(PALETTE).map((color) => (
          <AxFlexBox.Col key={color} flex="auto">
            <AxAvatar title="Username" icon={mdiAccount} color={color} />
          </AxFlexBox.Col>
        ))}
      </AxFlexBox.Row>
      <AxFlexBox.Row>
        {Object.values(PALETTE).map((color) => (
          <AxFlexBox.Col key={color} flex="auto">
            <AxAvatar title="Username" icon={mdiAccount} bg={color} />
          </AxFlexBox.Col>
        ))}
      </AxFlexBox.Row>
    </AxFlexBox>
  </Story>
</Canvas>

<Heading>Props</Heading>

<ArgsTable of={AxAvatar} sort="requiredFirst" />
