import {
  Anchor,
  ArgsTable,
  Canvas,
  Description,
  Heading,
  Meta,
  Story,
  Title
} from "@storybook/addon-docs";
import { ModalStory } from "./ModalStory";
import { AxModal } from "../../src";

<Meta title="@core/Panels/Modal" component={AxModal} parameters={{ jest: ["Modal.test.tsx"] }} />

<Anchor storyId="core-panels-modal--modal">
  <Title>Modal Overlays</Title>
</Anchor>

<Description of={AxModal} />

<Canvas>
  <Story name="Modal">{ModalStory}</Story>
</Canvas>

<div className="ax-margin--b--lg" />

```tsx
import { AxBox, AxButton, AxContent, AxModal } from "@axux/core";
import { useState } from "react";

export const ModalBody = ({ doClose }) => {
  return (
    <AxModal onClose={doClose}>
      <AxModal.Header title="Modal title" icon="mdi mdi-bell" iconBg="primary" />
      <AxContent>Any content</AxContent>
      <AxModal.Footer>
        <AxButton onClick={doClose} />
      </AxModal.Footer>
    </AxModal>
  );
};

export const Element = () => {
  const [isOpen, setOpen] = useState(false);

  const showModal = () => {
    setOpen(true);
  };

  return (
    <AxBox>
      <AxButton onClick={showModal}>Show Modal</AxButton>
      {isOpen && <ModalBody />}
    </AxBox>
  );
};
```

<Heading>Props</Heading>

<ArgsTable story="Modal" sort="requiredFirst" />
