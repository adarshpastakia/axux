import {
  Anchor,
  ArgTypes,
  Canvas,
  Controls,
  Meta,
  Subheading,
} from "@storybook/blocks";
import { AxApplicationProvider } from "../../src";
import * as ViewportStories from "./Viewport.stories";
import * as ErrorBoundaryStories from "./ErrorBoundary.stories";

<Meta of={ViewportStories} />

# Viewport

Basic application viewport

---

<Anchor storyId="viewport">
  <Subheading>AxViewport</Subheading>
</Anchor>

<Canvas of={ViewportStories.Example} />

<Anchor storyId="context">
  <Subheading>AxApplicationProvider</Subheading>
</Anchor>

<ArgTypes sort="requiredFirst" of={AxApplicationProvider} />

<Anchor storyId="context">
  <Subheading>AxErrorBoundary</Subheading>
</Anchor>
<Canvas of={ErrorBoundaryStories.ErrorBoundary} />
<Controls of={ErrorBoundaryStories.ErrorBoundary} />

<Anchor storyId="hook">
  <Subheading>useApplicationContext</Subheading>
</Anchor>

```ts
import { useApplicationContext } from "@axux/core";

const Component = () => {
  const {
    /**
     * close all open overlays
     * @aparam includeNotifications boolean
     */
    closeOverlays,
    /**
     * toggle theme
     * @aparam "dark" "light"
     */
    toggleTheme,
    /**
     * change locale
     */
    changeLocale,
    /**
     * change default calendar
     * @param "gregorian" "hijri"
     */
    changeCalendar,
    currentTheme,
    currentLocale,
    currentCalendar,
  } = useApplicationContext();

  .
  .
  .
};
```
