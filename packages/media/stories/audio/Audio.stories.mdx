import { Title, Canvas, Story, ArgsTable } from "@storybook/addon-docs";
import { AudioStory } from "./AudioStory";
import { AxAudioPlayer } from "../../src";

<Meta
  title="@media/Audio Player"
  component={AxAudioPlayer}
  parameters={{ jest: ["Audio.test.tsx"] }}
/>

<Title>Audio Player</Title>

<Canvas>
  <Story name="Audio Player" height="32rem" parameters={{ layout: "none" }}>
    {AudioStory}
  </Story>
</Canvas>

> ### Drawing boxes and hilighting areas

```tsx
import { AxAudioPlayer, AudioPlayerRef } from "@axux/media";

const AudioTemplate = () => {
  const AudioRef = useRef<AudioPlayerRef>(null);

  // play audio region
  AudioRef.current.playRegion(0.18, 1.35);
  
  return <AxAudioPlayer src="..." ref={AudioRef} />;
};
```

<ArgsTable sort="requiredFirst" of={AxAudioPlayer} />
